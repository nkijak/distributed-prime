Demo of distributed prime number calculation.  To compute more numbers faster just open up the index page in more browsers.

* npm install
* node server


(in browser)
http://localhost:3010

click "Go!"

